"""Pure matcher registry for deterministic security rules.

Concrete matchers are added separately from the rule-engine contracts so the
engine can be tested without any benchmark or model dependency.
"""


RULE_MATCHERS = ()
